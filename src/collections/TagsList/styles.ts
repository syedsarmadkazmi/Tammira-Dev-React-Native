import { StyleSheet } from 'react-native';
import { HPX } from '~app/services';

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        paddingVertical: HPX(10),
    },
});
